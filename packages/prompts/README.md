# AI prompts

AI prompt definitions will live in this package when AI features are introduced. Prompts must not be scattered throughout mobile or API application code.

No real prompts are included during the foundation stage. Gemini integration will be implemented later through an AI provider interface so application logic does not depend directly on a single vendor.
