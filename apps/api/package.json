{
  "name": "@anlat-hoca/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "build": "wrangler deploy --dry-run --outdir dist",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrations:list:local": "wrangler d1 migrations list DB --local",
    "generate-types": "wrangler types",
    "typecheck": "wrangler types --check && tsc --noEmit"
  },
  "dependencies": {
    "@anlat-hoca/config": "workspace:*",
    "@anlat-hoca/contracts": "workspace:*",
    "@anlat-hoca/prompts": "workspace:*",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "~6.0.3",
    "wrangler": "^4.129.0"
  }
}
