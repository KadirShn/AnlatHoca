{
  "name": "@anlat-hoca/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "build": "wrangler deploy --dry-run --outdir dist",
    "generate-types": "wrangler types",
    "typecheck": "wrangler types --check && tsc --noEmit"
  },
  "dependencies": {
    "@anlat-hoca/contracts": "workspace:*",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "~6.0.3",
    "wrangler": "^4.129.0"
  }
}
